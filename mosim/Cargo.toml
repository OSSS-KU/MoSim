[package]
name = "mosim"
version = "0.1.0"
edition = "2021"
license = "MIT"

[dependencies]
csv = "1.3"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
opt-level = 3
lto = true
